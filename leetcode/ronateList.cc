#include<iostream>
using namespace std;
struct node
{ int v;
	struct node *next;
};
node* Renodte(node* head)
{
		node *tail=NULL;
		node *pre=NULL;
		node *next=NULL;
		while(pre->next)
		{
				if(!tail)
					tail-pre;
		}
}
int main()
{
	
	head=NULL;
	node *tmp=Renote(head);	
	return 0;
}
