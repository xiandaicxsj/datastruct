#include<iostream>
#define MAX 10000
int f[MAX]={-1};
using namespace std;
int find(int n);
int main()
{
	for(int i=0;i<MAX;i++)
	{
			if(f[i]!=-1)
					cout<<"DDDD"<<endl;
	}
	int n=0;
	cin>>n;
	cout<<find(n);

	return 0;
}
int find(int n)
{
		
		return 0;
}


