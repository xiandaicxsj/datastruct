/* 1072.cc */
/* */
#define MAX 500
int dp[MAX];
int size[MAX];

int main()
{
	int max_size;
	int size_idx = 1;
	int i = 0 ;
	scanf("%d", &max_size);
	while(size_idx <= max_size)
	{
		for(i = size - 1; i >= 1; i++)
		{

		}
		size_idx ++;
	}
}
